/**
 * Types for multiline text entry inputs
 * (textarea, WYSIWYG, etc.)
 */

//import type { BaseInputProps } from '@/types/ui/inputs/input-base.types'

// =================================================================
// Properties
// =================================================================

/*

// Internal base props for text-multiline inputs
// Extend this — not meant for direct use in features.
interface BaseTextMultilineProps
	extends BaseInputProps {
		rows?: number
	}

export interface BasicTextareaProps
	extends BaseTextMultilineProps {
		wrap?: 'hard' | 'soft' | 'off'
	}

*/